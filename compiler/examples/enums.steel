
# of new types, as labels
enum Option[T] {
    struct Some(T),
    struct None,
}

# of existing types (must be structs or enums, not interface)
struct Apple {}
struct Mushroom {}
enum Produce {
    Apple,
    struct Banana {},
    enum Vegetable {},
    Option[Mushroom],
}
